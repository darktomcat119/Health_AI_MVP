"use client";

import { ChatContainer } from "@/components/chat/ChatContainer";

/**
 * Main chat page. Renders the full chat interface
 * including messages, input, and streaming support.
 */
export default function ChatPage() {
  return <ChatContainer />;
}
